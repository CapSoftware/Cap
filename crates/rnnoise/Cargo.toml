[package]
name = "cap-rnnoise"
version = "0.1.0"
edition = "2024"
license = "BSD-3-Clause"

[build-dependencies]
cc = "1"

[lints]
workspace = true

[dependencies]
workspace-hack = { version = "0.1", path = "../workspace-hack" }
