[package]
name = "scap-ffmpeg"
version = "0.1.0"
edition = "2024"

[dependencies]
ffmpeg = { workspace = true }

[target.'cfg(windows)'.dependencies]
scap-direct3d = { optional = true, path = "../scap-direct3d" }
scap-cpal = { optional = true, path = "../scap-cpal" }

windows = { workspace = true, features = [] }

[target.'cfg(target_os = "macos")'.dependencies]
scap-screencapturekit = { optional = true, path = "../scap-screencapturekit" }

cidre = { workspace = true }

[dev-dependencies]
futures = { workspace = true }
tokio = { workspace = true }
cap-displays = { path = "../displays" }

[lints]
workspace = true
