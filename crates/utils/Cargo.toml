[package]
name = "cap-utils"
version = "0.1.0"
edition = "2021"

[target.'cfg(unix)'.dependencies]
nix = { version = "0.29.0", features = ["fs"] }

[target.'cfg(target_os = "windows")'.dependencies]
winapi = "0.3.9"
