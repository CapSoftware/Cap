use cap_recording::CameraFeed;
use cap_utils::Url;
use std::collections::HashMap;

pub mod cap_recording {
    pub struct CameraFeed {
        // implementation
    }

    impl CameraFeed {
        pub fn new() -> Self {
            Self { /* implementation */ }
        }
    }
}